{
    "settings": [
      {
        "provider": "cloudflare",
        "zone_identifier": "${DDNS_UPDATER_ZONE_IDENTIFIER}",
        "domain": "traunseenet.com",
        "host": "@",
        "ttl": 600,
        "token": "${DDNS_UPDATER_TOKEN}",
        "ip_version": "ipv4"
      }
    ]
  }